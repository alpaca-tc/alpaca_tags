#!/bin/sh

git_root_dir=`git rev-parse --show-toplevel`

echo $@
if [ -d "${git_root_dir}" ]; then
    cd ${git_root_dir}
    bundle show --paths | xargs ctags $@ -R -f ${git_root_dir}/.git/gem.tags
fi
