#!/bin/sh

git_root_dir=`git rev-parse --show-toplevel`

if [ -d "${git_root_dir}" ]; then
    echo $1
    cd ${git_root_dir}
    /Applications/MacVim.app/Contents/MacOS/ctags $1 -f ${git_root_dir}/.git/tags ${git_root_dir}
fi
